dataset_name: hirid
raw_dataset_version: 1.1.1

urls:
  dataset:
    - url: https://physionet.org/content/hirid/get-zip/${raw_dataset_version}/
      username: ${oc.env:DATASET_DOWNLOAD_USERNAME}
      password: ${oc.env:DATASET_DOWNLOAD_PASSWORD}
  demo:
    - EXAMPLE_OPEN_URL
  common:
    - EXAMPLE_SHARED_URL # Often used for shared metadata files
